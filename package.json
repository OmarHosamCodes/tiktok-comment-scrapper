{
  "name": "tiktok-comment-scrapper",
  "version": "2.0.0",
  "description": "TikTok Comment Scrapper",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "playwright": "^1.57.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "typescript": "^5.7.2"
  }
}
